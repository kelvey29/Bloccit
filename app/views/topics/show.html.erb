<h1><%= @post.title %></h1>
 
 <div class="row">
   <div class="col-md-8">
     <p><%= @topic.description %></p>
   </div>
   <div class="col-md-4">
     <%= link_to "Edit", edit_sponsored_post_path(@topic.sponsored_post, @topic), class: 'btn btn-success' %>
     <%= link_to "Delete Post", [@topic.sponsored_post, @post], method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this post?'} %>
   </div>
 </div>>
 </div>